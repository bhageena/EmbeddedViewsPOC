<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Page1</title>
</head>
<body>
</body>
</html>

@if (Model.HasData)
{
    <span class="item-string" id="@Model.Name">
        @if (Model.ShowLabel && !String.IsNullOrEmpty(Model.Label))
        {
            @Model.Label<text>:</text>
        }
        <span class="item-value">@Model.Value</span>
    </span>
}